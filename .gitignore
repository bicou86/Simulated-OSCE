node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Variables d'environnement (clés API — ne jamais committer)
.env
.env.local
.env.*.local

# Répertoire local du harnais Claude Code
.claude/

# Logs JSONL générés par le logger d'observabilité
server/logs/

# Previews éphémères Phase 3 (non committés — artefacts de travail)
tmp/phase3-previews/

# Phase 6 — sorties du script de triage médico-légal. Le run de
# référence J1 (`phase-6-j1.csv`) est commité pour traçabilité ; les
# runs ultérieurs (j2.csv, j3.csv, debug.csv) restent locaux.
triage-output/*.csv
!triage-output/phase-6-j1.csv
