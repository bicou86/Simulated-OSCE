# Clés API (fournies par l'utilisateur via l'écran Paramètres ou renseignées ici)
# Copier ce fichier en .env.local puis compléter. .env.local est ignoré par git.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Port HTTP du serveur Express (sert aussi le client Vite en dev)
PORT=5000
